# Integration test summary

The RAG suite passed **290/290** tests. Added `/retrieve` cases check strict request fields, Top-K, frozen chunk provenance, rank and similarity, no generation invocation, and invalid lifecycle startup rejection. Existing `/query`, startup validation, policy, lifecycle, hash, count, dimension, and runtime tests remained green. The local private service started successfully; four independent `/retrieve` queries plus a Top-K probe matched the official frozen artifact's chunk IDs, document/section metadata, physical pages, original text, ranks, normalized content hashes, and finite similarities. No retrieval benchmark was run.

The selected Agent document-workflow, reliability, and offline research suites passed **271/271** tests. Added cases cover `/retrieve` HTTP mapping, stable chunk Evidence identity across different queries, cache deduplication and citation membership, and bounded PARTIAL outcomes for connection refusal, timeout, 5xx, unavailable artifact, invalid lifecycle response, and malformed payloads. The old simulated workflow, NoProgress, MISSING, renderer, and tool-policy tests also passed. Live research and sandbox suites were outside this selected regression scope.

The safe real-HTTP E2E executed five section tasks and eight `/retrieve` calls, stored 14 unique Evidence items, reused hits in later tasks, emitted four valid draft citations, marked throughput MISSING, and produced `draft.docx`, `evidence.json`, and `execution_trace.json`. A RAG-side read-only check matched all 14 emitted Evidence records to safe artifact chunk ID, document ID, section ID/path, physical page, normalized content, and hash. The original template SHA256 remained `c933d53ae806bdc3b937e30879e800e07a00f01c496df1b3b8eead21f1ced2e4`.

Microsoft Word exported the draft to a one-page PDF for QA. Its rendered page was inspected: headings, paragraphs, four citations, the table, and visible MISSING marker were readable without clipping or overlap. The packaged LibreOffice renderer was unavailable on this Windows host, so the existing local Word export helper and PDFium rasterization were used.
