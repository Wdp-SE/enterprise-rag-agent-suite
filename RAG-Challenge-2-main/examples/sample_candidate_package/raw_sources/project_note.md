# Local source snapshot

The Generic Retriever keeps every FAISS index independent. It retrieves a
local Top-K from each document, merges those candidates, and then selects the
global Top-K. This file is a synthetic interface example, not an industry or
production knowledge source.
