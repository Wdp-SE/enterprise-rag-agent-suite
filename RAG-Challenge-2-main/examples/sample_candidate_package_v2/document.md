# Hash-aware Candidate Interface Sample

The v2 Candidate Contract keeps the downloaded source file inside
`raw_sources/` and declares a byte-level SHA-256 digest for that file
[source:S01].

This package demonstrates the interface only; it is not production knowledge.
