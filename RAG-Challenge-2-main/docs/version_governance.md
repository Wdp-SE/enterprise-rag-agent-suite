# 版本治理

DocumentCatalog 保存稳定文档身份和版本状态。每个 document_id 最多有一个 ACTIVE 版本，其余版本为 SUPERSEDED。

写入新版本时：

1. 校验稳定元数据和版本身份。
2. 计算源文件哈希，拒绝重复版本与重复源文件。
3. 校验章节路径在版本内唯一。
4. 先写版本资产和活动索引。
5. 最后原子替换 catalog.json。
6. 激活新版本并将旧活动版本标为 SUPERSEDED。

目录提交放在最后，构建失败不会破坏原活动版本。Frozen runtime 可加载显式版本目录，也可从冻结语料清单构造只读目录。
