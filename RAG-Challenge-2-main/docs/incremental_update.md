# 增量更新

VersionLifecycleService 对规范化章节内容计算 SHA-256。向量缓存以内容哈希为键：

- 哈希已存在：直接复用向量。
- 哈希不存在：仅对新增或修改内容调用 embedder。
- 删除章节：不进入新版本资产。
- 写入完成：根据所有 ACTIVE 版本刷新精确向量矩阵索引。

IncrementalBuildReport 记录章节变更数、复用向量数、新向量数、活动分块数、耗时和索引刷新策略。向量在落盘前会校验维度、有限值和单位范数。
